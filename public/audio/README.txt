Place your looping time-announcer file here as:

  timetrack.mp3

The app auto-loads /audio/timetrack.mp3 and loops it.
The Web Audio analyser samples its volume every 100ms and
ducks the user's music queue (Track B) whenever speech is
detected.
